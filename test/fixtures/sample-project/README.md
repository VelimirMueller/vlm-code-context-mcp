# Sample Project

A minimal fixture project used for testing the code-context indexer.

## Structure

- `src/index.ts` — Application entry point
- `src/utils/` — Utility and helper functions
- `src/services/` — Service layer (API client, etc.)
- `src/types.ts` — Shared TypeScript types and interfaces
